/**
 * Tipos canónicos de la app.
 *
 * La fuente única es la captura generada directamente desde Supabase.
 * No agregar parches manuales aquí: si cambia el esquema, regenera
 * `database.generated.ts`.
 */
export type { Database } from './database.generated';
