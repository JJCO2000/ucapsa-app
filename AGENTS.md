# Expo HAS CHANGED

This repository currently uses Expo SDK 57 / React Native 0.86. Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing Expo-specific code.

For Android edge-to-edge and system controls, preserve safe-area handling with `react-native-safe-area-context`; do not solve overlap by hardcoding a device-specific navigation-bar height.
